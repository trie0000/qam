{
  "name": "qam",
  "version": "0.1.0",
  "description": "Qualys Asset Management — local asset registration & change-history tool (TS app + thin PowerShell relay)",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "test": "vitest run",
    "typecheck": "tsc"
  },
  "devDependencies": {
    "@xmldom/xmldom": "^0.9.10",
    "esbuild": "^0.21.0",
    "happy-dom": "^14.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
